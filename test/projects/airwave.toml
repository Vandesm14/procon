source.git = "https://github.com/vandesm14/airwave"
# source.path = "."
# souce.zip = "$HOME/downloads/my-project.zip"

deps.modules = ["rust"]
deps.nix = ["just", "mprocs"]

phase.build = ["cargo build"]
phase.start = "just dev"
